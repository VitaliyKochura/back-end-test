{% extends 'base.html.twig' %}

{% block title %}{{ 'Home' }}{% endblock %}

{% block body %}
    <div class="container my-4 p-4 text-center">
        <h1>Resume Database Management Application</h1>
        <p class="lead">Choose what you want to do:</p>
        <a class="btn btn-primary mx-2" href="{{ path('company_index') }}">View a list of all companies</a>
        <a class="btn btn-primary mx-2" href="{{ path('resume_index') }}">View a list of all resume options</a>
    </div>
{% endblock %}